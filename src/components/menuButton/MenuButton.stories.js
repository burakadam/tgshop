import React from "react";

import MenuButton from "./MenuButton";

export default {
  component: MenuButton,
  title: "MenuButton",
};

export const Primary = () => (
  <div style={{ width: 150 }}>
    <MenuButton />
  </div>
);
